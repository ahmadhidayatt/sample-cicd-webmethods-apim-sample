
node {
    checkout([
        $class: 'GitSCM',
        branches: [[name: "*/${config.repoBranch}"]],
        userRemoteConfigs: [[
            url: config.repoUrl,
            credentialsId: config.repoCredId
        ]]
    ])
    loadProperties()
    deploy()
    summary()
}
